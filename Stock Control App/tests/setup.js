import { expect, afterEach, vi } from 'vitest';

// Cleanup after each test (if needed for future React component tests)
afterEach(() => {
  // Cleanup will be added when we test React components
});

// Note: Window and DOM mocks are not needed for parser function tests
// They will be added when we test React components with jsdom environment

